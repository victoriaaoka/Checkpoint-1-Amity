language: python
python:
    - 3.4
install:
    - pip install -r requirements.txt
    - pip install coveralls
    - pip install pytest-cov

script:
   - py.test --cov-report=term --cov=test_cases
after_success: coveralls
