language: python
python:
    - 3.4
install:
    - pip install -r requirements.txt
    - pip install coveralls
    - pip install pytest-cov

script:
    - coverage run --omit=*/test_cases/* --source=my_app pytest-cov test_cases
after_success: coveralls
