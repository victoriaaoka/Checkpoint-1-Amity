language: python
python:
    - 3.4
install:
    - pip install -r requirements.txt
    - pip install coveralls
    - pip install pytest-cov

script:
  coverage run --source=test_cases manage.py test
after_success:
  coveralls
